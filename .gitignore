__pycache__/
*.pyc
*.egg-info/
.venv/
.pytest_cache/
.coverage
dist/
build/
mapping.yaml
events.jsonl
pipeline-state.yaml
AGENTS.local.md
.claude/settings.local.json

# mage runtime state (Plan 15 — approval gate marker lives here)
.mage/

# per-plan scratch artifacts (task reports, review diffs) live in the
# project notes directory at ~/code/project-notes/mage/superpowers/sdd/
# — never in the repo
/.superpowers/
